# ברבור טורס

מלווה נסיעות אישי שעובד קודם כל אופליין. טיול אחד בכל רגע נתון, בעברית, בטלפון,
במדינה שבה הנדידה עשויה לעבוד ועשויה שלא. כל החלטת תכנון בפרויקט הזה נגזרת מהסיפא
של המשפט הקודם.

**בעלים:** BARBURai (רון). שני חשבונות קלוד עובדים על הריפו הזה, בתורות — ראה
[הסכם העבודה](#הסכם-העבודה).

> הקובץ הזה בעברית כי הבעלים קורא ועורך אותו. הקוד עצמו — הערות והודעות קומיט —
> נשאר באנגלית.

## כללי הברזל

הכללים האלה נכתבו ב-21.09.2026 אחרי שהבעלים נאלץ לגלות בעצמו ש-19 קואורדינטות בטיול
היו שגויות — אחת מהן 4.5 ק״מ מהמקום, וכמה מהן בתוך הים. **הוא ביקש שזה לא יקרה שוב,
לעולם.** זה לא סגנון, זה תנאי.

### 1. קואורדינטה נכנסת רק ממקור. אין ניחוש.

לכל `lat`/`lng` שאתה כותב חייב להיות אחד משלושת המקורות האלה, ואין רביעי:

| מקור | איך |
| --- | --- |
| **המפה** | `npm run find -- "<שם>" --near <עיר>` — מחזיר את הנקודה מ-OpenStreetMap |
| **הבעלים** | לחיצה ארוכה בגוגל מפות ושליחת המספרים |
| **נגזרת** | אותה נקודה של מקום אחר שכבר מאומת (הספא = המלון) — ותכתוב בהערה ממה נגזר |

**אסור בהחלט:** מספר מהזיכרון, מספר שהוסק מתיאור מילולי, ומספר מרשימה שמישהו העביר.
הרשימה שהבעלים העביר ב-21.09 נראתה סבירה והכילה מרחק שגוי פי שניים; חלק
מהקואורדינטות בה היו מאות מטרים מהמקום. **גם מה שהבעלים מעביר עובר `npm run find`** —
זה לא חוסר אמון בו, זה שהרשימה לא בהכרח שלו.

אם `npm run find` לא מוצא את המקום — **אין לו נקודה.** הוא מקבל `search` ואתה אומר
לבעלים במפורש שהוא לא אומת. `To Souvlaki Tou Soukri` הוא בדיוק המקרה הזה.

### 2. "אימתתי" מול תיאור זה לא אימות.

ב-21.09 סשן קודם "אימת" את תצפית הבלגונה מול תיאור מילולי, הזיז אותה שלושה קילומטרים,
וכתב ביומן שהאימות הצליח. היא נשארה 1.4 ק״מ מהפסגה האמיתית **ובתוך הים**. זה גרוע
מלא לבדוק, כי זה נראה כמו בדיקה.

**אימות הוא מול נתונים בלבד:** `npm run find`, `npm run land`, `npm run places`.
השוואה לטקסט, ל"הגיון גיאומטרי" או לזיכרון — אינה אימות, ואסור לקרוא לה כך.

### 3. אסור לכתוב מרחק או זמן הליכה בפרוזה. תחשב.

שלוש טענות מרחק היו שגויות ב-21.09 ("10 דק׳ הליכה מהמרינה" היה 2 ק״מ). מרחק שנכתב
ביד מתיישן ברגע שנקודה זזה, ואף אחד לא חוזר לעדכן אותו. אם צריך מרחק — **תחשב אותו
מהקואורדינטות** לפני שאתה כותב, ואם הוא יכול להשתנה, שהאפליקציה תחשב אותו בזמן ריצה.

### 4. `./check` הוא שער, לא הצעה.

```
./check                 # מול הטיול החי
./check trip.json       # מול קובץ, בלי רשת
```

מילה אחת משורש הריפו. מתקין מה שצריך לבד בפעם הראשונה, מריץ את הכל — preview, bidi,
land, places, trip — ומחזיר חמש שורות בעברית. **אין מיזוג ל-`main`
ואין כתיבה לענן לפני שהוא ירוק.** אם משהו נכשל ואתה בכל זאת מוסר — תגיד את זה בפירוש
לבעלים, עם הסיבה. **לעולם לא לעבור FAIL בשקט.**

הכלים האלה היו קיימים לפני שהנתונים נשברו. אף אחד לא הריץ אותם כי אף אחד לא היה חייב.

### 5. חריג הוא החלטה כתובה, לא דרך לצבוע ירוק.

שתי נקודות בטיול **אמורות** להיראות שגויות: העגינה בבלו לגון יושבת במים (סירה עוגנת
במים), והסיכה של Alpha Divers מצביעה למרינה ולא לכתובת הרשומה (החלטת הבעלים). אילו
נשארו כ-FAIL, השער היה אדום לנצח — **ושער שתמיד אדום הוא שער שמפסיקים לקרוא.**

לכן יש `tools/water-exceptions.json` ו-`tools/place-exceptions.json`, וכל שורה בהם
נושאת **סיבה**. הכלל: אם אתה לא מסוגל לכתוב סיבה שמטייל היה מקבל — זה לא חריג, זה באג.
אסור להוסיף שורה שם כדי שהריצה תעבור.

### 6. כלי שמדווח שטות — זורקים אותו, לא מתעלמים ממנו.

שלוש גרסאות של בדיקות נזרקו ב-21.09 כי דיווחו על טקסט תקין: אחת פיצלה `16:30` לשני
מספרים, אחת בלעה `לילה 1: Leonardo`, ואחת קשרה `Alpha Divers` לסוכנות הימורים בשם
`Bet on Alpha`. **בדיקה שצועקת על מה שתקין מאמנת אותך להתעלם ממנה**, ואז אתה מפספס את
האמיתית. תצמצם עד שכל דיווח הוא אמיתי.

**וכל בדיקה חדשה חייבת להיבדק שהיא נכשלת:** תזריק את הבאג שהיא אמורה לתפוס ותוודא
שהיא נכשלת עליו. `npm run bidi` נבדק כך מול שתי המחרוזות ההיסטוריות. בדיקה שלא ראית
אותה נכשלת היא בדיקה שלא בדקת.

### 7. הבעלים הוא לא ה-QA.

כל באג שהוא מצא ב-21.09 — הכפתור המת, המצפן הלבן, חיפוש המלונות, הנקודות בים — היה
נמצא בדקה אם מישהו היה לוחץ. **תפתח את המסך, תלחץ על הכפתורים, תקרא את התוצאה.**
"עובר קומפילציה" ו"קראתי את הדיף" אינם בדיקה.

## מה זה

אפליקציית PWA של עמוד אחד, מוגשת כקבצים סטטיים מ-**GitHub Pages** ישירות מהריפו
הזה: דחיפה ל-`main` היא הדיפלוי, אין שלב בנייה ואין CI. הטיולים חיים ב-Firestore
(פרויקט `barbur-tours`, אוסף `tours`); כל השאר מקומי. אין באנדלר, אין מנהל חבילות,
אין סוויטת בדיקות. עורכים את `index.html` — וזה הדיפלוי.

```
index.html        כל האפליקציה - 3.5 אלף שורות
sw.js             service worker (קאשינג של ה-shell, ניתוב אופליין)
manifest.json     מניפסט ה-PWA
cyprus.pmtiles    מפת בסיס Protomaps לטיול קפריסין, 49MB
vendor/           maplibre-gl, pmtiles, ערכות protomaps, תתי-קבוצות של פונט Rubik
icon-192/512.png  אייקוני האפליקציה
```

שום דבר לא נטען מ-CDN חוץ משני מודולי ה-ES של Firebase, שגרסתם מקובעת. ספריות
המפה והפונטים יושבים ב-`vendor/` **בכוונה**: אפליקציה שעובדת אופליין לא יכולה
להישען על כך ש-CDN יהיה נגיש, וה-service worker יכול לשמור בקאש רק מה שהוא מסוגל
למשוך מאותו origin. אל "תמודרן" קובץ מקומי בחזרה לקישור CDN.

**מי משתמש בזה:** קודם כל הבעלים, ובזמן טיול גם המטיילים איתו (טיול קפריסין הוא
טיול גברים — רון, איתן ואורי). אין הרשמה, אין התחברות, אין ריבוי משתמשים: מי שפותח
את הקישור רואה את הטיול.

### מה מגן על הנתונים

מפתח ה-API של Firebase יושב בצד הלקוח, כמו שחייב להיות ב-web-app — הוא מזהה את
הפרויקט, הוא לא סוד, ואי אפשר "להסתיר" אותו. מה שמגן בפועל הוא חוקי Firestore, והם
אלה (פורסמו 19.09.2026):

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tours/{tripId} {
      allow read: if true;
      allow create, update: if true;
      allow delete: if false;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

עד אז החוקים היו `allow read, write: if true` על כל המסד, בלי תאריך תפוגה — כלומר
כל אוסף, לכל אחד, לקריאה ולמחיקה.

מה שהם עושים, ומה שלא:

- **מחיקה חסומה לגמרי.** זה הנזק היחיד שאי אפשר לתקן, ולכן הוא היחיד שנחסם מוחלט.
  **המשמעות: `deleteCurrentTrip()` באפליקציה מחזיר שגיאה.** זה מכוון. מוחקים טיול
  מהקונסולה. אל "תתקן" את זה בהחזרת `allow delete`.
- **שאר המסד סגור.** רק `tours` נגיש; כל אוסף אחר מחזיר 403.
- **קריאה וכתיבה ל-`tours` פתוחות.** מי שמוצא את הפרויקט יכול לקרוא ולערוך את תוכן
  הטיולים. בלי מנגנון הזדהות אין דרך לחסום את זה — זו מגבלה ידועה ומקובלת, לא פספוס.
  הפתרון אם אי פעם ירצו: הזדהות אנונימית של Firebase (בלי מסך התחברות) וחוקים
  שדורשים `request.auth != null`.

> **הריפו ציבורי, וזו החלטה מודעת של הבעלים.** בתוך הזריעות ב-`index.html` יושבים
> 12 קודי הזמנה אמיתיים (PNR), מספרי מושבים ו-23 קישורי Google Drive למסמכי הטיול.
> הועלה ב-19.09.2026, נשקל, והבעלים בחר להשאיר כך. **אל תעלה את זה שוב** ואל תציע
> להפוך את הריפו לפרטי — זה כבר נדון ונסגר. רק תדע את זה כשאתה מוסיף נתוני טיול
> חדשים, כדי שתהיה החלטה ולא הפתעה.

## המבנה של index.html

שלושה בלוקים, בסדר הזה:

| שורות (בערך) | מה |
| --- | --- |
| 15-110 | `<script type="module">` — אתחול Firebase וה-API של `window.DB`. סקריפט המודול היחיד. |
| 112-642 | `<style>` — כל ה-CSS, כולל `@font-face` וטוקני ערכת הנושא. |
| 646-823 | ה-markup — כותרת, מגירה, עשרת בלוקי `<section class="view">`, מודל הטיול וניווט תחתון. |
| 825-3566 | `<script>` — כל קוד האפליקציה, קלאסי (לא מודול), ולכן כל handler הוא גלובלי. |

מספרי השורות זזים בכל קומיט. נקודות הציון היציבות הן הערות הסקשנים בסקריפט הראשי,
בצורת `// ---------- Name ----------`. תתמצא לפיהן:

`Drawer` · `Navigation` · `Offline map` · `Theme` · `Offline state` · `Trip status helper`
· `Render drawer trip list` · `Select trip` · `Empty state` · `Trip form` · `Helpers`
· זריעות חד-פעמיות · `Itinerary rendering` · `Bookings rendering` · `Currency rendering`
· `Packing rendering` · `Prep checklist` · `Phrases` · `Emergency` · `Transport`
· `Notifications` · `Init`

### המסכים

עשרה סקשנים, אחד גלוי בכל רגע, מוחלפים על ידי `navigate(view)`:
`home` `itinerary` `bookings` `currency` `packing` `prep` `phrases` `emergency`
`transport` `map`.

חמישה מהם נמצאים בניווט התחתון (בית, יומן, הזמנות, מפה, מה לקחת); לשאר מגיעים
מהמגירה. **המגירה היא הניווט האמיתי** — הניווט התחתון הוא קיצור דרך, לא מפת
האפליקציה.

`navigate` דוחף `history.pushState({view}, '')` **בלי פרמטר URL** — GitHub Pages
זורק `SecurityError` אם מנסים לשנות את ה-URL. אל תוסיף שם פרמטר.

### המצב

גלובל אחד: `STATE = { currentView, currentTripId, tours, editingTripId, selectedFlag }`.
`curTour()` מחזיר את הטיול הנוכחי. אין פריימוורק, אין ריאקטיביות ואין עץ קומפוננטות:
מסך משתנה כי פונקציית `render*(tour)` כותבת מחדש את ה-`innerHTML` של המכל שלו. תשאיר
את זה ככה אלא אם הבעלים מבקש אחרת — חצי פריימוורק יהיה גרוע יותר מכלום.

## נתונים

`tours/{id}` ב-Firestore, נקרא ונכתב אך ורק דרך `window.DB`
(`listTours` `getTour` `saveTour` `deleteTour` `watchTour`). `saveTour` עושה merge
(`setDoc` עם `{merge:true}`), ולכן מסך יכול לשמור רק את הפרוסה שלו (`saveDays`,
הכותבים של רשימת הציוד וההכנות). **אזהרה:** merge מחליף מערך שלם ולא ממזג בתוכו —
אם תכתוב `bookings` חלקי אתה מוחק את השאר. תמיד תכתוב את המערך המלא של הפרוסה שאתה
נוגע בה.

הקריאה במצב אופליין מוגנת פעמיים: ההתמדה של Firestore עצמו ב-IndexedDB היא הקו
הראשון, ועותק `toursSnapshot` ב-`localStorage` הוא הקו השני — למקרה ש-IndexedDB
פונה או מעולם לא אוכלס במכשיר הזה. `DB.fromCache` ו-`DB.lastSync` מזינים את פס
ההתיישנות. טיול לעולם לא יהיה פשוט לא-זמין בחו"ל.

הטיולים נזרעים על ידי `window.seedCyprus` / `seedGeorgia` / `seedKorea` (כפתורים
במגירה, תחת "זריעות חד-פעמיות"). כל פונקציה כותבת ל-Firestore אובייקט טיול שלם עם
`dataVersion`. הטבלה `TRIP_SEEDS` (שורה 1697) מחזיקה את הגרסה שהקוד מצפה לה לכל
טיול; אם הגרסה ב-Firestore נמוכה מזו שבקוד, מסך הבית מציג כפתור עדכון שמריץ מחדש
את הזריעה המתאימה.

> **הכפתור הזה דורס.** הרצת זריעה מחדש כותבת את היומן, ההכנות ורשימת הציוד מהקוד —
> כל מה שהמשתמש סימן באפליקציה נמחק. הטקסט בכרטיס אומר את זה למשתמש; תזכור את זה
> גם כשאתה מציע לו ללחוץ.

> **מוקש מרכזי:** שינוי אובייקט הזריעה בקוד **לא** מעדכן את Firestore בעצמו.
> Firestore ממשיך להגיש את העותק הישן עד שמישהו לוחץ על כפתור העדכון / "טען מחדש".
> אחרי כל שינוי נתונים: תעלה את `dataVersion` באובייקט **ובטבלה** `TRIP_SEEDS`,
> אחרת הבעלים לא יראה את השינוי ולא תהיה שום אינדיקציה שצריך לרענן.

> **סדר הפעולות, ואין ממנו חריגה: קודם הקוד לאוויר, אחר כך הנתונים בענן.**
> ב-19.09 נכתבו נתונים במבנה חדש (`total`) בזמן שהטלפון עוד הריץ קוד שמכיר רק
> `perPerson`. המסך הציג `€undefined × 3` והסכום היה שגוי. הענן משותף לכל המכשירים
> ומתעדכן מיד; הקוד מתעדכן רק אחרי דיפלוי ורענון. לכן כל שינוי במבנה הנתונים חייב
> לרוץ בסדר: מיזוג ל-`main` → לוודא שהדיפלוי הסתיים → ורק אז לכתוב לענן.

מקורות חיצוניים: Open-Meteo לתחזית (חינם, בלי מפתח, נשמר בקאש — תחזית ישנה עדיפה
על פאנל ריק; יש נפילה ל-geocoding לערים לא מוכרות), ושער המרה למסך המטבע.

## אופליין וה-service worker

`CACHE` ב-`sw.js` הוא גרסת הדיפלוי. **תעלה אותו בכל קומיט
שמשנה קובץ שמופיע ברשימת `SHELL`**, אחרת משתמשים חוזרים נשארים עם העותק הישן;
`activate` מוחק כל קאש שאינו ה-`CACHE` הנוכחי.

> **מוקש היסטורי:** ה-service worker נרשם **מיד עם טעינת הדף**, לא בתוך `init()`.
> פעם הרישום ישב בתוך `init()` שרץ רק על אירוע `db-ready` של Firebase — אם gstatic
> לא נגיש, ה-worker לא נרשם אף פעם והאופליין פשוט לא עבד. יש גם נפילה של 6 שניות
> ל-`toursSnapshot` מ-localStorage. אל תחזיר את הרישום לתוך `init()`.

שלושה חריגים מכוונים ב-handler של `fetch`, לכל אחד יש הערה עם הסיבה מעליו — תקרא
אותן לפני שאתה נוגע בקובץ:

- `googleapis.com` עובר ישירות (יירוט שלו שובר גם את הקאש הפנימי של Firestore וגם
  את ערוץ ההאזנה ארוך-הטווח שלו).
- `.pmtiles` עובר ישירות (בקשות Range מייצרות תשובות 206 ש-Cache API מסרב לאחסן;
  מסך המפה שומר את הארכיון כולו ב-IndexedDB בעצמו).
- ניווטים הם network-first עם ה-shell מהקאש כגיבוי; כל מה שמאותו origin הוא
  cache-first.

ההתקנה משתמשת ב-`Promise.allSettled` ולא ב-`cache.addAll`, כדי שבקשה אחת שנכשלת לא
תשאיר משתמש בלי שום עותק אופליין.

## המפה

ארכיון `.pmtiles` אחד שנקרא ישירות על ידי ספריית pmtiles — בלי שום שרת אריחים.
המשתמש מוריד אותו פעם אחת ל-IndexedDB ממסך המפה, ומשם זה עובד בלי קליטה. GPS,
פונקציות עזר למרחק וכיוון, סיכות ממוספרות לפי יום, מסך מלא, ושכבת מסלול אופציונלית
(כבויה כברירת מחדל). הכל יושב תחת סקשן `Offline map`.

המפה נפתחת על **כל הטיול**, והצ'יפים למעלה מסננים ליום בודד. `BOOKING_PINS` קובע
אילו קטגוריות הזמנה הופכות לסיכות — מלונות ורכב, לא טיסות (טיסה אינה מקום; שדה
התעופה כבר ביומן). מקום מוזמן בלי `lat`/`lng` מוצג ברשימה תחת "אין קואורדינטות,
ולכן לא על המפה" במקום להיעלם בשקט. לכל נקודה שני כפתורי ניווט: "נווט" הולך ישר
ל-Google, ו-📍 שולח `geo:` כדי שאנדרואיד יציע כל אפליקציית מפות מותקנת — בשטח
ל-Google אין מסלול ול-Maps.me או Organic Maps יש.

איך נוצר `cyprus.pmtiles`: **לא** הורדה מהאתר — צריך את ה-CLI של Protomaps
(`pmtiles extract`) כדי לחתוך אזור מתוך ה-build העולמי. הבעלים ייצר את הקובץ מקומית
ודחף אותו דרך git. אם צריך מפה לטיול חדש, זה תהליך של הבעלים במחשב, לא משהו
שהאפליקציה יכולה לייצר. ראה [החלטות ומוקשים](#החלטות-ומוקשים) למה לא Google Maps.

## קונבנציות

- **עברית, RTL.** `<html lang="he" dir="rtl">`. כל מחרוזות הממשק בעברית. מספרי טלפון
  צריכים את `linkPhones` כדי להישאר קריאים ב-RTL — אל תכתוב את זה שוב לבד. מספרים עם
  סימנים (מטבע, טלפון) צריכים `unicode-bidi: isolate` אחרת ה-bidi הופך אותם.
- **ערכת נושא.** כהה היא ברירת המחדל; בהירה קיימת עם כפתור החלפה. לעולם אל תקבע צבע
  קשיח — תשתמש בטוקנים שעל `:root` (`--bg --surface --surface-2 --accent --accent-2
  --text --text-dim --text-faint --border --red --green --blue --radius --radius-sm
  --shadow`) ובדריסות שלהם תחת `:root[data-theme="light"]`.
- **טלפון קודם.** פורטרט, `viewport-fit=cover`, standalone. תבדוק כל שינוי ברוחב של
  טלפון (360×600 בערך) לפני שאתה אומר שסיימת.
- **הערות מסבירות למה, לא מה.** ההערות הקיימות מתעדות החלטות ואת הבאגים שגרמו להן.
  תתאים את עצמך לרישום הזה, ותשמור עליהן כשאתה נוגע בקוד סביבן.
- **אנגלית בריפו** (הערות קוד והודעות קומיט), עברית בממשק. הקובץ הזה הוא החריג.
- **הודעות קומיט** מתארות מה השתנה עבור המשתמש, במילים פשוטות — למשל
  "Make the drawer the navigation, and stop the home screen sprawling".

## החלטות ומוקשים

מה שסשן חדש ישבור בלי לדעת. אל תבטל אף אחד מאלה בלי סיבה טובה — כל אחד נולד מבאג
אמיתי או מבקשה מפורשת של הבעלים.

### גישות שנפסלו (אל תציע אותן שוב)

- **פריימוורק / ריאקטיביות.** נשאר וניל בכוונה. חצי פריימוורק גרוע מכלום.
- **CDN.** ספריות מפה ופונטים מקומיים ב-`vendor/` בכוונה (ראה "מה זה").
- **מפות Google / My Maps בתוך האפליקציה.** הבעלים ביקש מפה אופליין עם ניווט
  ונקודות. אי אפשר להטמיע מפות אופליין של Google או My Maps ולנווט בהן בלי לצאת
  מהאפליקציה, ואי אפשר להוסיף להן נקודות תוכנתית. הפתרון הנבחר: Protomaps `.pmtiles`
  + MapLibre GL, הכל מקומי. זה מה שיש — אל תחזור להצעת Google.

### באגים חוזרים והתיקון הנכון

- **חישוב יום התחזית:** תשתמש ב-`localDateStr()` והשווה מול `daily.time` של התחזית
  עצמה. אל תשתמש ב-`Math.round((dayDate - now)/86400000)` (נופל יום אחורה אחרי
  הצהריים) ואל ב-`toISOString()` (מזיז יום ב-UTC).
- **מספרי טלפון הפוכים:** `unicode-bidi: isolate` + `linkPhones()`. כפתורי WhatsApp
  רק למספרים בינלאומיים אמיתיים.
- **רשימת מספרים מופרדת בפסיקים בתוך טקסט עברי מתהפכת.** קרה פעמיים:
  `(35.03961, 32.39958)` הוצג כ-`(32.39958, 35.03961)`, ו-`(18, 21, 55)` הוצג הפוך.
  ה-bidi מסדר מחדש רצף מספרים שמופרדים בפסיקים בהקשר RTL. **הכלל:** אל תכתוב רשימת
  מספרים בפרוזה עברית. תצמיד כל מספר למילה ("איתן בן 18, אורי בן 21"), או תשתמש
  ב-`·` במקום פסיק. קואורדינטות בכלל לא שייכות לפרוזה — הן שייכות ל-`lat`/`lng`
  של הפריט, שם יש להן סיכה וכפתור ניווט.
  **`innerText` לא יתפוס את זה** — הוא מחזיר סדר לוגי, לא ויזואלי. מה שכן תופס:
  `npm run bidi` (ראה למטה), שמודד את המיקום בפיקסלים. הוא נבדק מול שתי המחרוזות
  ההיסטוריות והוא נכשל עליהן, ובריצה הראשונה שלו הוא מצא שתי תקלות חיות שאיש לא שם לב
  אליהן. לספרה שפותחת שם לטיני הוא עיוור — שם עדיין רק צילום מסך.
- **פס אופליין כתום וריק בכל מסך:** צריך `.offline-bar[hidden]{display:none}` —
  `display:flex` מנצח את `[hidden]`.
- **שדות קלט של מטבע גולשים:** `min-width:0; width:100%` — קלט מספרי לא מתכווץ
  מתחת לרוחב הטבעי שלו ב-flex.
- **`<details>` לא נסגר:** Chromium מסתיר תוכן דרך `::details-content`; צריך
  `.fold:not([open]) > *:not(summary){display:none !important}`.
- **מפה ממסגרת יום רחב מדי:** יום בודד ממסגר רק את הפעילויות שלו. מלונות (יום `all`)
  ו-GPS נכללים בגבולות רק ב"הצג הכל".
- **שכבת מסלול לא עושה כלום מתחת ל-zoom 12:** השכבה מציירת מ-z12. כפתור המסלול
  עושה auto-ease ל-zoom 13. השכבה כבויה כברירת מחדל.
- **הטיול שנפתח בדיפלוט היה גיאורגיה ישנה:** `lastTripId` שמור מכובד רק אם הטיול
  לא עבר. טיול שהסתיים לא ייבחר אוטומטית.

### העדפות הבעלים (מהשיחות)

- **פונטים גדולים** בכל האפליקציה. הועלה כל קנה המידה ~12%.
- **המגירה חייבת להיכנס במסך אחד בלי גלילה** — היא הניווט, ולכן גלילה בה זה כשל.
- **המגירה היא הניווט**; המפה (לא המטבע) בפס התחתון.
- **לא להציע שוב ושוב את הודעות ההזמנה** ("תעזוב את ההודעות האלה"). אם כבר הצעת
  משהו והוא סירב — זה סופי.
- **הבעלים לא רוצה לבדוק בעצמו שוב ושוב.** תעבור על ה-UI לבד, תצלם מסכים, תריץ
  סימולציות, ותציע שיפורים יזומים — אל תהיה ריאקטיבי.
- **בלוק "הטיול מתחיל בעוד"** לא צריך להיות ריבוע ענק וריק — פעם היה דקורטיבי עם
  תמונה, שיישאר קומפקטי ואינפורמטיבי.
- כשמדווחים על עבודה: תגיד בפירוש מה **כן** בדקת ומה **לא** בדקת. בלי הבטחות שווא.

## נתוני הטיול הפעיל (נכון למסירה)

חי ב-Firestore, כאן רק לצורך התמצאות מהירה — אל תשכפל את זה כמקור אמת.

- **קפריסין** (`cyprus-2026`) — 1–5.10.2026, טיול גברים: רון, איתן, אורי. לרנקה,
  פאפוס והאקמאס. מטבע EUR. זה הטיול הפעיל/הדיפולט. את `dataVersion` הנוכחי תקרא
  מ-`TRIP_SEEDS` שבקוד, לא מכאן — מספר שמועתק לכאן מתיישן תוך קומיט.
  [תיקיית דרייב](https://drive.google.com/drive/folders/1YrgnwwKX238gc1haoDcJli8L8jMD0lwp).
- **גאורגיה** (`georgia-2026`) — עבר, שמור לעיון.
- **קוריאה** (`korea-2026`) — 17.6–1.7.2026 (רון והילה), עבר.
- **טרם הוזמן** לקפריסין: סירה (€300 סה״כ), צלילות (€140 לאדם), רייזרים
  (€200 סה״כ), קרטינג. רכב (Sixt), מלונות (Leonardo, WellClub) וטיסות — הוזמנו.
  הזמנת Ajax בוטלה.

## לראות את זה לפני שזה מגיע לטלפון

`tools/preview.mjs` פותח את האפליקציה בכרומיום אמיתי ברוחב טלפון, מגיש את **הקבצים
שעל הדיסק** (לא את האתר החי), מזריק את נתוני הטיול האמיתיים מ-Firestore דרך מסלול
הגיבוי האופליין של האפליקציה עצמה, ומצלם כל מסך שתבקש.

```
cd tools && npm install          # פעם אחת
npm run preview                                  # מסך הבית
npm run preview -- --views home,itinerary,map    # כמה מסכים
npm run preview -- --theme light
npm run preview -- --snapshot trip.json          # בלי רשת בכלל
```

יש גם `tools/bidi-check.mjs` (`npm run bidi`), שעובר על כל המסכים ומודד **איפה כל מספר
באמת נחת על המסך**, לא איפה הוא נכתב. הוא מחפש את הצורה שנפלה כאן פעמיים — שני מספרים
או יותר שמחוברים בפסיק או בנקודה-פסיק בתוך פרוזה עברית — ומדווח רק כשהיא באמת נפרשה
הפוך. **הוא לא תופס** ספרה שפותחת שם לטיני (`7 St. George's Tavern`), כי אותה צורה בדיוק
נכונה ב"לילה 1: Leonardo" ורק המשמעות מבדילה ביניהן; שם עדיין צריך להסתכל בתמונה.

**ושלושה כלים שמשתמשים ב-`cyprus.pmtiles` עצמו כמקור אמת גיאוגרפי.** הקובץ הזה הוא
נתוני OpenStreetMap, הוא כבר בריפו, הוא עובד בלי רשת — וזו בדיוק המפה שהאפליקציה
מציירת, ולכן תשובה ממנו היא תשובה על מה שהמטייל יראה. `tools/pmtiles-lib.mjs` קורא
אותו ישירות (PMTiles v3 + Mapbox Vector Tiles), בלי רינדור — הניסיון לאמת קואורדינטות
דרך צילום המפה נכשל כאן בעבר והחזיר קנבס ריק, ולגיאומטריה וקטורית אין את הבעיה הזו.

- `npm run land` — שכבת `earth` היא מצולע היבשה. כל נקודה בטיול נבדקת אם היא בתוכה,
  ומדווח **המרחק במטרים** ליבשה הקרובה, כי "בים" בלי מספר לא אומר כלום כשזה ארבעה
  מטרים. יש ארבע נקודות ביקורת (ניקוסיה, טרודוס, ושני מקומות בים פתוח) והכלי **עוצר**
  אם אחת מהן יוצאת הפוך.
- **`npm run find -- "<שם>" --near <עיר>` — זה שממנו קואורדינטות נכנסות.** מחפש את
  השם בשכבות `pois`/`places`/`landuse` ומחזיר את הנקודה מוכנה להדבקה. אם הוא לא מוצא,
  למקום אין נקודה — נקודה. (הגרסה הראשונה שלו החזירה מכולות רוסיות בתשובה ל-"Psariko",
  כי שם בלי אותיות לטיניות מתנרמל למחרוזת ריקה ומחרוזת ריקה "מוכלת" בכל שאילתה.)
- `npm run places` — שכבת `pois` מחזיקה שמות. לכל מקום בעל שם בטיול הכלי מוצא את
  ה-POI הקרוב עם אותו שם ומדווח כמה אנחנו רחוקים ממנו. **התאמת שמות היא המקום שבו
  זה נשבר:** גרסה ראשונה קשרה את `Alpha Divers` ל"Bet on Alpha" (סוכנות הימורים)
  ואת `Ristorante Bacco` לשלוש חנויות טבק. לכן נדרשת גם מילה מבחינה בת ארבע אותיות
  ומעלה שאינה מילת קטגוריה, **וגם** שסוג המקום יתאים (מסעדה למסעדה, לא לבוקמייקר).

**`npm run trip` — הבדיקה שמסתכלת על הטיול ולא על האפליקציה.** כל השאר בודקים אם
המסך נטען ואם הסיכות על היבשה; אף אחד מהם לא שואל אם *התוכנית* מחזיקה. טיול יכול
להיות עם סיכות מושלמות ועדיין לשים אותך בשני מקומות באותו זמן, להשאיר לילה בלי מיטה,
או להחזיק רכב שכבר הוחזר. הבדיקה הזו קוראת את הטיול כמו סוכן נסיעות:

יום בשבוע מול התאריך האמיתי · שעות בסדר עולה · **מרחק מול הפער בלו״ז** (מעל 80 קמ״ז
בקו אווירי זה בלתי אפשרי בכביש, לא "צפוף" — טיסות פטורות) · מיטה לכל לילה · רכב פעיל
כשהתוכנית נוסעת · יעדי הכנה שעברו · טיסות בתוך טווח התאריכים · שום שדה ריק בשקט.

**כל מה שהיא מדווחת הוא חשבון על הנתונים של הטיול עצמו**, ולכן תלונה שלה אף פעם אינה
עניין של טעם.

**`./check` משורש הריפו מריץ את כולם** ומחזיר חמש שורות בעברית.
זה השער מ"כללי הברזל": אין מיזוג ואין כתיבה לענן לפני שהוא ירוק.

הצילומים נוחתים ב-`.preview/` (ב-gitignore). הכלי נכשל עם קוד יציאה שונה מאפס אם
יש שגיאת JS, אם קובץ מקומי מחזיר 404 (פונט או ספרייה חסרים = האופליין שבור), או אם
מסך מציג `undefined` / `NaN` / `[object Object]`. כשלי רשת חיצוניים (gstatic,
open-meteo) **אינם** כישלון — הם בדיוק מצב חוסר-הקליטה שהאפליקציה בנויה לשרוד.

הכלי לא יגיד לך אם המסך *יפה*. זה מה שהצילומים בשבילם — תסתכל עליהם.

> למה זה קיים: ב-19.09 נשלח לטלפון של הבעלים מסך שהציג `€undefined × 3`. שני
> החשבונות "בדקו" את השינוי — אחד קרא את הדיף, השני הריץ בדיקת תחביר — ואף אחד לא
> הסתכל על המסך, כי אף אחד לא יכול היה. הבעלים גילה את זה. אין כאן סוויטת בדיקות
> ולא צריכה להיות; הבדיקה שקובעת היא להסתכל.

## טיול חדש — סדר הפעולות

הכאב ב-21.09 נבע מכך שהנתונים נכתבו קודם והקואורדינטות נוחשו, ורק אחר כך התברר שהן
שגויות. **הסדר הזה הופך את זה:** קודם המפה, ואז כל נקודה נשלפת ממנה. זה גם מדויק וגם
מהיר יותר, כי אין סבב תיקונים.

**1. הבעלים מייצר את קובץ המפה של האזור — לפני הכל.**

```
pmtiles extract <world-build> <trip>.pmtiles --bbox=<minLng,minLat,maxLng,maxLat>
```

זה צעד של הבעלים במחשב (ה-CLI של Protomaps), והוא **חוסם**: בלעדיו אין `npm run find`,
אין `npm run land` ואין `npm run places` — כלומר אין דרך לאמת שום נקודה, וכל הטיול חוזר
להיות ניחוש. **אל תתחיל לכתוב נתוני טיול בלי הקובץ.** בזמן שאין קובץ, מקומות נכנסים עם
`search` בלבד ובלי נקודה.

**2. כל מקום נשלף מהמפה, אחד-אחד.**

```
npm run find -- "Porto Latchi" --near latchi
```

מה שחוזר זה מה שנכנס. מקום שלא נמצא — אין לו נקודה, ומצוין לבעלים שהוא לא אומת.

**3. כותבים את היומן, ההזמנות ורשימות האוכל.** בלי מרחקים בפרוזה (כלל 3).

**4. שער:**

```
./check <seed>.json
```

**5. מסתכלים על הצילומים ב-`.preview/`.** הכלי לא יודע אם המסך יפה.

**6. מיזוג ל-`main` → לוודא שהדיפלוי הסתיים → ורק אז כתיבה לענן.**
(הסדר הזה הוא חוק בפני עצמו, ראה "נתונים".)

**7. לעדכן `dataVersion`, `TRIP_SEEDS`, `CACHE` ו-`lastUpdate`** — ארבעתם, באותו קומיט.

### כשכותבים לענן, לא דורסים את מה שהמשתמש סימן

`prep` ו-`packing` מחזיקים את הסימונים של הבעלים. **לעולם אל תכתוב אותם מתוך הזריעה
כדי לתקן שדה אחד.** קרא את המסמך החי, שנה את המחרוזת הבודדת, וכתוב בחזרה — ואז ספור
את הסימונים לפני ואחרי ותגיד את המספר. ב-21.09 זה שמר שמונה סימונים שהיו נמחקים.

ב-`updateMask` שולחים **רק** את השדות שבאמת השתנו. שדה שלא השתנה לא נשלח בכלל — זו
הדרך הבטוחה ביותר לא לדרוס אותו.

## הסכם העבודה

שני חשבונות קלוד עובדים על הריפו הזה. **`main` הוא מקור האמת היחיד, ועובדים
בתורות** — אף אחד לא מתחיל בזמן שהתור של השני פתוח. כמעט כל הקוד יושב בקובץ אחד,
ולכן עבודה במקביל פירושה קונפליקטים בתוך סקריפט של 2,700 שורות, וזה בדיוק המקום
שבו דברים נשברים בשקט.

בכל תור:

1. סנכרן קודם: `git fetch origin main && git checkout -B <הענף שלך> origin/main`.
   לעולם אל תבנה על `main` ישן.
2. משימה אחת = קומיט אחד = PR אחד = סקווש-מרג' ל-`main`. בלי ענפים ארוכי-טווח.
3. תעלה את `CACHE` ב-`sw.js` אם השינוי נוגע בקובץ מ-`SHELL`.
4. **`npm run check` ירוק לפני המיזוג, וצילום מסך בכל מסירה.** לא "עובר קומפילציה",
   לא "קראתי את הדיף" — צילום, ושער ירוק. נכשל ואתה בכל זאת מוסר? תגיד את זה בפירוש.
5. **אימות הצולב הוא שלב בתור, לא נימוס.** כשאתה פותח תור אחרי החשבון השני, תבדוק
   מול הקוד את מה שהוא כתב לפני שאתה בונה עליו. זה לא חוסר אמון: לשני החשבונות יש
   זיכרונות שונים של הפרויקט, ושניהם בטוחים בעצמם באותה מידה. ב-19.09 נכתב כאן
   בביטחון על טבלה בשם `DATA_VERSIONS` שלא קיימת בקוד (שמה `TRIP_SEEDS`) — האימות
   של התור הבא הוא שתפס את זה.
6. תעדכן את יומן המסירה למטה, באותו קומיט.
7. תמסור את התור במפורש.

תגיד בפירוש מה **כן** בדקת ומה **לא** בדקת. "בדקתי" בלי לפרט מה נבדק הוא הבטחה
ריקה, והבעלים ביקש מפורשות שלא.

## יומן מסירה

החדש למעלה. רשומה אחת לכל תור: מה השתנה, באילו סקשנים, ומה נשאר פתוח.

- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — **"כל התהליך חייב להיות
  ממש ממש פשוט", ו"לא רק הנקודות על המפה".** שתי בקשות, שתי תשובות.
  **`./check` — מילה אחת משורש הריפו.** מתקין תלויות לבד בפעם הראשונה, מריץ את חמש
  הבדיקות, ומחזיר **חמש שורות בעברית** במקום קיר של אנגלית. `./check trip.json` רץ
  מול קובץ בלי רשת.
  **`tools/trip-check.mjs` (`npm run trip`) — הבדיקה שהסתכלה על הטיול ולא על האפליקציה.**
  כל שאר הבדיקות שואלות אם המסך נטען ואם הסיכות על היבשה. אף אחת לא שאלה אם **התוכנית**
  מחזיקה. היא בודקת: יום בשבוע מול התאריך האמיתי, שעות בסדר עולה, **מרחק מול הפער
  בלו״ז** (מעל 80 קמ״ש בקו אווירי = בלתי אפשרי בכביש; טיסות פטורות), מיטה לכל לילה,
  רכב פעיל כשנוסעים, יעדי הכנה שעברו, טיסות בטווח, ושדות ריקים.
  **הריצה על הטיול האמיתי:** חמש מתוך חמש עוברות. הערה אחת שווה מבט — **2.10, מ-19:00
  (איסוף Sixt) ל-20:45 (צ׳ק-אין ב-WellClub): 111 ק״מ אווירי ב-105 דקות, 64 קמ״ש.**
  זה תואם לאזהרה שכבר כתובה ביום ההוא ביד, וזו אינדיקציה טובה שהסף מכויל נכון.
  **נבדק שהיא נכשלת** (כלל 6), שש דרכים: יום בשבוע שגוי, שעה שיורדת, שני לילות בלי
  מלון, יעד שעבר, ונסיעה של 119 ק״מ ב-45 דקות — כל אחת נתפסה, יציאה 1. **ושתיים
  מההזרקות הראשונות שלי לא היו באמת באגים** (שעה שעדיין עולה, ויעד על משימה שכבר
  מסומנת) — הכלי צדק שהוא שתק, וזה מה שגרם לי לחזור ולהזריק נכון.

- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — **הבעלים ביקש שזה לא
  יקרה שוב, לעולם: "שיהיה זריז קל ומדויק".** נוסף סעיף **כללי הברזל** בראש הקובץ,
  וסעיף **טיול חדש — סדר הפעולות**.
  **הכלל המרכזי:** קואורדינטה נכנסת רק משלושה מקורות — `npm run find` (מהמפה),
  לחיצה ארוכה של הבעלים, או נגזרת ממקום מאומת אחר. **מהזיכרון, מתיאור מילולי, או
  מרשימה שמישהו העביר — אסור.** גם רשימה שהבעלים מעביר עוברת `npm run find`; זו
  שהוא שלח ב-21.09 הכילה מרחק שגוי פי שניים וקואורדינטות שהיו מאות מטרים מהמקום.
  **`tools/find-place.mjs` (`npm run find`)** — זה שממנו נקודות נכנסות מעכשיו.
  **`tools/check-all.mjs` (`npm run check`)** — שער אחד שמריץ preview + bidi + land +
  places ומחזיר PASS/FAIL. אין מיזוג ואין כתיבה לענן לפני ירוק.
  **הסדר לטיול חדש התהפך:** קודם הבעלים מייצר את ה-`.pmtiles` של האזור, ורק אז נכתבים
  נתונים — כי בלי הקובץ אין `find`, אין `land`, אין `places`, וכל נקודה חוזרת להיות
  ניחוש. זה גם מהיר יותר: אין סבב תיקונים.
  **שני קבצי חריגים** (`water-exceptions.json`, `place-exceptions.json`) עם **סיבה**
  לכל שורה, כי העגינה בבלו לגון והסיכה של Alpha Divers אמורות להיראות שגויות, ושער
  שתמיד אדום הוא שער שמפסיקים לקרוא.
  **מה נבדק, וזו הבדיקה שקובעת:** בניתי snapshot **שבור בכוונה** — נקודה שנדחפה לים
  כמו שפיניקודס הייתה, מסעדה שהוזזה כמו Psaropoulos, ושתי מחרוזות ה-bidi ההיסטוריות —
  והרצתי את השער. **שלוש מתוך ארבע הבדיקות נכשלו, יציאה 1.** על הנתונים האמיתיים:
  ארבע מתוך ארבע עוברות, יציאה 0. כלומר השער מבדיל בין טיול תקין לשבור, ולא סתם ירוק.
  **באג שנמצא ותוקן תוך כדי:** `npm run find` בגרסתו הראשונה החזיר מכולות רוסיות
  בתשובה ל-"Psariko" — שם בלי אותיות לטיניות מתנרמל למחרוזת ריקה, ומחרוזת ריקה
  "מוכלת" בכל שאילתה. בדיוק אותו כשל שהכלים האלה נועדו למנוע.

- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — שלוש תשובות מהבעלים,
  ואחת מהן שינתה בוקר שלם. **Alpha Divers אוספים מהמלון ב-07:30** — כלומר אין הליכה
  למרינה, והצ׳ק-אאוט שהיה ב-08:00 **התנגש עם האיסוף** והוקדם לשבע. שאלת המפגש
  (מרינה או פילה) נסגרה: מרינה, וממילא אוספים. שאלת האשראי נסגרה גם היא.
  נוספה משימה לבקש מ-PR Rentals את אפליקציית הניווט שלהם בהגעה לדלפק.
  `dataVersion` ל-20, `CACHE` ל-`v39`.
  **הדפוס שחוזר:** תשובה מספק לא רק סוגרת שאלה, היא לרוב מזיזה את הלו״ז. כששומעים
  "הם אוספים ב-07:30" צריך מיד לבדוק מה עוד יושב בבוקר הזה — כאן זה היה צ׳ק-אאוט
  שחפף לאיסוף.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — ארבע נקודות המסלול
  באקמאס אומתו מחדש מול גוגל מפות, **ושתיהן שסימנתי כחשודות אכן היו שגויות:**
  מרחץ אפרודיטה זז כקילומטר מזרחה ויושב עכשיו 74 מ׳ מהמקור העצמאי שמצאתי (היה 920),
  והתצפית זוהתה כפסגת **Moutti Tis Sotiras**, זזה כשלושה ק״מ, ויושבת עכשיו 1.84 ק״מ
  מהבלגונה במקום 4.31 — מרחק שתצפית באמת יכולה להשקיף ממנו. לארה זזה 220 מ׳
  ו-Viklari כקילומטר. הלולאה: 43 ק״מ בקו אווירי, כ-69 בשבילים. `dataVersion` ל-19,
  `CACHE` ל-`v38`.
  **הלקח על השיטה:** בדיקת המרחקים מול נתונים שכבר קיימים בטיול עבדה. שתי הנקודות
  שנראו לא הגיוניות גיאומטרית היו באמת שגויות, והשתיים שנראו סבירות כמעט לא זזו.
  **תמיד תריץ את הבדיקה הזו על קואורדינטות חדשות** — היא זולה והיא תופסת.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — Ploutarxos ענה על
  הביטוח, וזה סגר שאלה ופתח אחת גדולה יותר. **התשובה:** ביטוח צד ג׳ כלול במחיר,
  פיקדון מוחזר של €1000 בדלפק (כרטיס או מזומן), והשתתפות עצמית של €8000 במקרה אובדן
  מוחלט. חלון ההגעה התרחב עד 10:00, ויש להם אפליקציית ניווט משלהם לשבילי הפארק.
  **מה שזה חשף:** Sixt תופסת €800 מ-2.10 עד 5.10, ו-PR Rentals תופסת עוד €1000
  ב-3.10 — **שתי התפיסות חיות באותו זמן על אותו כרטיס.** צריך מסגרת פנויה של כ-€1800
  או פיצול בין שניים. נוספה משימת הכנה. זה סוג הדבר שנראה רק כששמים את שתי ההזמנות
  זו לצד זו, ולכן שווה לבדוק חפיפות פיקדונות בכל טיול.
  `dataVersion` ל-18, `CACHE` ל-`v37`.
  **הבעלים שאל אם הוא צריך ללחוץ על כפתור כדי לעדכן — התשובה היא לא**, והוא צריך
  להישאר לא. הכרטיס הירוק הוא הודעה בלבד; הנתונים מגיעים מהענן מעצמם. כרטיס הזריעה
  הכתום דורס סימונים ולכן אסור שיופיע — זו הסיבה שמסנכרנים `dataVersion` בכל עדכון.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — **הבעלים אמר ששתי דברים
  לא נראים לו: שנקודות במפה מובילות לים, ושנקודות המסעדות לא קשורות למה שגוגל מראה.
  הוא צדק בשניהם, והממצא גדול בהרבה ממה שהוא תיאר.**
  נבנו שתי בדיקות מעל `cyprus.pmtiles` (ראה למעלה), ואיתן נבדקה **כל נקודה בטיול**.
  **19 קואורדינטות היו שגויות.** הגדולות:
  | מה | היה | האמת לפי OSM | שגיאה |
  | --- | --- | --- | --- |
  | עגינה בבלו לגון (4.10) | 35.0556 / 32.3406 | 35.08390 / 32.30558 | **4.5 ק״מ** |
  | תצפית הבלגונה (3.10) | 35.0717 / 32.3361 | 35.08271 / 32.30634 | **4.3 ק״מ**, ובים |
  | Viklari | 34.9083 / 32.3278 | 34.91966 / 32.33476 | **1.4 ק״מ** |
  | Psaropoulos | 35.0425 / 32.3956 | 35.03935 / 32.39948 | 497 מ׳ |
  | Souvlaki.GR | 34.9142 / 32.3365 | 34.91125 / 33.63642 | 328 מ׳ |
  | מצודת לרנקה | 34.9083 / 33.6392 | 34.91028 / 33.63767 | 260 מ׳, ובים |
  | טיילת פיניקודס (4 פריטים) | 34.9128 / 33.6404 | 34.91325 / 33.63840 | 183 מ׳, ובים |
  **הדפוס, וזה הלקח:** כל נקודה שהבעלים שלח הייתה על היבשה ובמקום; כל נקודה שסשן
  קודם **הסיק** מפרוזה או מ"אימות" מול תיאור מילולי נחתה בים או קילומטרים משם.
  הנקודה של Moutti Tis Sotiras "אומתה" בסבב קודם והוזזה שלושה ק״מ — והיא עדיין הייתה
  1.4 ק״מ מהפסגה האמיתית ובתוך הים. **אימות מול תיאור אינו אימות. אימות הוא מול נתונים.**
  **עוד 10 מסעדות קיבלו נקודה מדויקת** ישירות מה-POI במפה (Zephyros, Stefanos,
  Psarolimano, Salamis, Aldente, Maqam Al-Sultan, Kasparis, Porto Latchi, Hondros,
  Seven St. Georges) — 16 מקומות מאומתים עכשיו עד 10 מ׳.
  **טענות מרחק שהתבררו כשגויות ותוקנו:** היומן כתב שטברנות פיאלה פאשה הן "10 דק׳
  הליכה מהמרינה" — הן כ-2 ק״מ משם. Militzis נכתב 155 מ׳ מהמצודה — 256. Souvlaki.GR
  נכתב 240 מ׳ מהמלון — 421.
  **שני דברים שנשארו ולא שיניתי:** (א) הסיכה של Alpha Divers מצביעה למרינה לפי
  החלטת הבעלים — ו-OSM מצביע, כמו גוגל, לפילה (34.97658 / 33.68242, `dive_centre`).
  זו החלטה סגורה ולא פתחתי אותה, רק מתעד שגם OSM מסכים עם גוגל. (ב) Sixt מצביע
  לאולם הנכנסים כפי שההזמנה אומרת; ל-OSM יש משרד Sixt 1.58 ק״מ משם.
  **ועוד אחד שנשאר בכוונה:** העגינה בבלו לגון יושבת 90 מ׳ בתוך המים — **וזה נכון**,
  זו עגינת סירה.
  `dataVersion` ל-25, `CACHE` ל-`v44`.
  **מה נבדק:** 103 כפתורי ניווט בחמשת הימים ובכל שאר המסכים — 57 ניווט ישיר, 35 למפה
  הפנימית, 11 חיפוש, **אפס מתים, אפס שגיאות JS**. `npm run land` נקי חוץ מהעגינה,
  `npm run places` 16 מתוך 16 תואמים, `npm run bidi` נקי.
  **מה לא נבדק:** `To Souvlaki Tou Soukri` ו-WellClub ו-PR Rentals אינם ב-OSM בשם,
  ולכן אי אפשר לאשר או להפריך אותם מכאן.

- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — **הבעלים לחץ על הספא
  וקיבל רשימת מלונות בגוגל.** הוא שלח צילום מסך, והבאג היה אמיתי.
  **מה קרה:** ל-`ספא ווולנס בריזורט` ב-3.10 לא היו קואורדינטות, אז הנפילה בנתה חיפוש
  טקסט בגוגל מתוך `item.activity + ' ' + day.location`. אבל `day.location` הוא **מסלול
  ולא מקום** — `"לאצ׳י → חצי האי אקמאס"` — והשאילתה שיצאה הייתה
  `"ספא ווולנס בריזורט לאצ׳י → חצי האי אקמאס"`. גוגל ראה "ספא/ריזורט" ופתח חיפוש מלונות
  בלאצ׳י. **שישה פריטים היו במצב הזה.**
  **התיקון, ובמפורש לא "לשפר את השאילתה":** לנחש מקום מתוך פרוזה עברית זו הטלת מטבע,
  וכפתור שנוחת במקום אקראי גרוע מאין כפתור — בדיוק הלקח של כפתור ה-`geo:` שהוסר.
  עכשיו: קואורדינטה מנווטת, שדה `search` מפורש (שנכתב כי ידוע שהוא נפתר) מחפש, וכל
  השאר **לא מקבל כפתור בכלל**. רשימת הסוגים
  `['sightseeing','food','hotel','car']` נמחקה — היא שימשה רק לשער את מקרה החיפוש.
  **ובמקביל הושלמו הנקודות החסרות:** הספא של שני הערבים, הצ׳ק-אאוט ושתי ה"חזרה
  ל-WellClub" קיבלו את הנקודה של WellClub; ארוחת הדגים של 4.10 והנסיעה אליה קיבלו את
  מרינת לאצ׳י; הקארטינג קיבל `search: "Paphos Karting Centre Geroskipou"`.
  מ-28 פריטים עם ניווט ל-**35 עם ניווט ישיר**, אחד עם חיפוש, ו-11 בלי כפתור — וכל
  אחד עשר הוא באמת לא יעד (המראה, ביטחון בשדה, "יציאה מזרחה לכיוון לרנקה").
  **תוקן גם שם שהיה שגוי:** `ארוחת דגים במרינה (Yiannakis / Periyiali)` — שתי טברנות
  שלא יושבות שם. השורה מפנה עכשיו לרשימת "איפה לאכול" של אותו יום.
  `dataVersion` ל-24, `CACHE` ל-`v43`.
  **מה נבדק:** הודפסו כל קישורי הניווט של 3.10 אחד-אחד ואומת שהספא מצביע ל-34.7881;
  היומן צולם; `npm run bidi` נקי; אפס שגיאות JS.
  **הכלל שנולד מזה:** נפילה שמנחשת גרועה מנפילה שמוותרת. אם אי אפשר לדעת לאן הכפתור
  ייקח — שלא יהיה כפתור.

- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — **שבע הנקודות שהבעלים שלח,
  ובדיקה חדשה שנולדה מהן.**
  כל שבע הנקודות נמדדו מול מה שכבר בטיול לפני שנכנסו, וכולן סבירות. שתי הערות:
  המרחק ל-`To Souvlaki Tou Soukri` הוא **322 מ׳ ולא 150** (כארבע דקות, לא שתיים),
  ו-`Ocean Basket` יושב כ-370 מ׳ מהמלון — מתאים למה שנמסר, אבל הוא כ-200 מ׳ פנימה
  מקו הטיילת ולא עליה.
  **הממצא המעניין:** טברנת קתיקאס יושבת כמעט בדיוק על הקו בין WellClub ללאצ׳י —
  13.9 ק״מ מהמלון ו-13.4 ק״מ מלאצ׳י, כשהמרחק הישיר הוא 27.2. כלומר עצירה שם מוסיפה
  לנסיעה כמאה מטר. לכן היא נכנסה לשני ימים, 3.10 ו-4.10, ולא לאחד.
  **`Viklari` — אי-התאמה שלא הוכרעה:** הנקודה שנשלחה עכשיו רחוקה 455 מ׳ מזו שאומתה
  בסבב הקודם מול קישורי גוגל של הבעלים. נשארה הישנה, כי היא זו שעברה אימות ייעודי.
  **מה השתנה בקוד:** מקום אכילה עם `lat`/`lng` מקבל עכשיו `🗺️ נווט` (ניווט ישיר) במקום
  חיפוש בגוגל. 11 מתוך 32 המקומות מפוענחים כך. `dataVersion` ל-23, `CACHE` ל-`v42`.
  **`tools/bidi-check.mjs` (`npm run bidi`) — הכלי שהקובץ הזה אמר שאי אפשר לבנות.**
  הכלל כאן היה "רק צילום מסך יתפוס היפוך bidi". זה נכון ל-`innerText`, אבל לא ל-`Range`:
  מדידה בפיקסלים של כל מספר אומרת בדיוק איפה הוא נחת. הכלי עובר על 14 מסכים (עשרת
  המסכים וחמשת ימי היומן), מחפש **שני מספרים או יותר שמחוברים רק בפסיק** בפרוזה עברית,
  ומדווח רק אם הם באמת נפרשו הפוך — צורה ומדידה יחד, ולכן דיווח שלו הוא תמיד אמיתי.
  **נבדק שהוא באמת נכשל:** הזרקתי את שתי המחרוזות ההיסטוריות
  (`(35.03961, 32.39958)` ו-`18, 21, 55`) והוא תפס את שתיהן. **שתי גרסאות קודמות שלו
  נזרקו כי הן דיווחו שטויות** — אחת פיצלה `16:30` לשני מספרים, השנייה בלעה
  `לילה 1: Leonardo` כרצף אחד. בדיקה שצועקת על מה שתקין מלמדת להתעלם ממנה.
  **ובריצה האמיתית הראשונה הוא מצא שתי תקלות חיות:** משימת ההכנה
  `להזמין סירה בלאצ׳י ל-4.10, 10:00-14:00` הציגה את השעה לפני התאריך, והערת הביטול של
  Leonardo (`עד 28.9; 3 ימים או פחות`) הוצגה הפוכה. שתיהן תוקנו ל-`·`.
  **מה נבדק:** חמשת ימי האוכל צולמו, שישה מסכים בריצת preview, `npm run bidi` נקי על
  14 מסכים, אפס שגיאות JS. **מה לא נבדק:** שהמסעדות פתוחות בתאריכים האלה.

- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — **איפה לאכול, בכל יום.**
  הבעלים ביקש רשימת מסעדות אמיתיות לכל יום, כולל יום הרייזר. לכל יום ב-`days` יש עכשיו
  `eat: {note, places[]}`, ו-`buildEatHtml(day)` מרנדר קיפול `<details class="fold">`
  בין ציר הזמן להערות. סך הכל 23 מקומות: חמישה בלרנקה ליום הנחיתה, שישה ליום הצלילה
  (פיאלה פאשה לצהריים, כלורקה לערב המאוחר), שלושה ליום הרייזר, חמישה ללאצ׳י ולערב
  הרגוע, וארבעה ליום האחרון.
  **אין לאף אחד מהם קואורדינטות, וזה מכוון:** לסמן 23 סיכות פירושו לבקש מהבעלים 23
  לחיצות ארוכות בגוגל מפות בשביל מקומות שאיש עוד לא התחייב אליהם. כל מקום מקבל קישור
  `mapSearchUrl()` — חיפוש בגוגל מפות לפי שם ועיר, שגוגל פותרת במדויק. זו אותה דרך
  שכבר עובדת להזמנה עם כתובת ובלי סיכה.
  **שלושה דברים שהתגלו תוך כדי:** (א) ליום 4.10 היומן כתב "Yiannakis / Periyiali",
  ושמות המסעדות שבאמת יושבות על נמל לאצ׳י הם Porto Latchi, Yiangos and Peter,
  ו-Psaropoulos — זה נרשם ב-`note` של היום. (ב) 5.10 הוא יום שני, וחלק מהמקומות
  בירוסקיפו סגורים בימי שני; זה נכתב בפתיח של היום. (ג) Viklari הוא תפריט אחד בלבד
  (בשר על שיפוד מסתובב), צהריים בלבד, נסגר בחמש — והטלפון שלו נכנס לכרטיס.
  **מוקש bidi שנתפס בצילום ותוקן:** השם `7 St. George's Tavern` הוצג כ-
  `St. George's Tavern 7`. ספרה בפתח רצף לטיני בהקשר RTL מקבלת את כיוון הפסקה ונזרקת
  לקצה השני. `unicode-bidi: isolate` לבדו לא מספיק — צריך גם `direction: ltr` על
  `.eat-name`, וזה מה שיש שם עכשיו. נמדד בדפדפן: התו הראשון של כל שם הוא השמאלי ביותר.
  `dataVersion` ל-22, `CACHE` ל-`v41`.
  **מה נבדק:** כל חמשת הימים צולמו עם הקיפול פתוח ונקראו מילה במילה; שישה מסכים בריצת
  `preview.mjs` מלאה, אפס שגיאות JS, אפס `undefined`. **מה לא נבדק:** שהמסעדות פתוחות
  בתאריכים האלה — השעות מגיעות מתוצאות חיפוש ולא מהמקומות עצמם, וזה כתוב לבעלים בפתיח
  של היום האחרון.

- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — שלוש בקשות של הבעלים על
  הניווט והתנועה באפליקציה.
  **(א) הכפתור השני שלא עשה כלום נמחק.** `mapNavigateAny()` שלח `geo:` כדי שאנדרואיד
  יציע כל אפליקציית מפות מותקנת. בטלפון של הבעלים זה פשוט לא עשה כלום — PWA במצב
  standalone לא מנווט את עצמו לסכימה שאף אחד לא מטפל בה, ואין שגיאה לתפוס. שני כפתורים
  שנראים שניהם כמו ניווט, כשאחד מהם מת, גרועים מאחד. נשארו שניים ברורים: `🗺️ נווט`
  החוצה לגוגל, ו-`📍 במפה שלנו` פנימה למפה האופליין.
  **(ב) כפתורי זום — והסיבה האמיתית שהם היו חסרים.** ל-MapLibre כבר היה
  `NavigationControl` בפינה השמאלית-עליונה, אבל כפתור המסך המלא של האפליקציה יושב בדיוק
  שם ומכסה אותו, ולכן כל מה שנראה ממנו היה מצפן לבן ולא-מעוצב בלי שום הסבר — זה מה
  שהבעלים ראה כשכתב "לא הבנתי את האופציות האלה". הקונטרול הוסר, ובמקומו `mapZoom(step)`
  עם שני כפתורים 44×44 בפינה השמאלית-תחתונה, בצבעי הערכה. **גם הסיבוב הוסר**
  (`dragRotate.disable()` + `touchZoomRotate.disableRotation()`): בלי מצפן אין דרך
  חזרה ממפה שהסתובבה בטעות בשתי אצבעות, ובשטח רוצים צפון למעלה.
  **(ג) סוויפ בכל האפליקציה.** `initGlobalSwipe()` על `document.body` מחליף את
  `initItinerarySwipe`, ו-`SWIPE_VIEWS = ['home','itinerary','map','packing']` הוא סדר
  הסרגל התחתון. ביומן הסוויפ קודם כל מדלג יום (`stepDay` מחזיר עכשיו בוליאני), וכשהימים
  נגמרים אותה תנועה ממשיכה למסך הבא. **מוקש שנתפס בבדיקה:** בגרסה הראשונה כל מסך המפה
  היה פטור מסוויפ — כלומר סוויפ הכניס למפה ואז שום סוויפ לא הוציא ממנה, לשני הכיוונים.
  הפטור צומצם ל-`.map-wrap` בלבד (הקנבס עצמו, שגרירה עליו היא הזזת המפה); הצ'יפים
  מעליו ורשימת הנקודות מתחתיו הם עמוד רגיל וסוויפ עליהם מנווט.
  `dataVersion` ל-21, `CACHE` ל-`v40`.
  **מה נבדק:** בכרומיום ברוחב טלפון — זום `7.11 → + → 8.11 → −− → 7.11`, הכפתורים
  44×44 ובתוך גבולות המפה, אפס קונטרולים של MapLibre בעמוד, סיבוב מושבת, מסך מלא נכנס
  ויוצא, ומסלול סוויפ מלא הלוך ושוב: `home → יומן (ימים 0-4) → מפה → מה לקחת` ובחזרה,
  בלי גלישה בקצוות; סוויפ על הקנבס לא מנווט, סוויפ על מסך המפה מחוץ לקנבס כן. חמישה
  מסכים צולמו, אפס שגיאות JS. **מה לא נבדק:** התנהגות מגע אמיתית באצבע על הטלפון —
  הבדיקות מדמות אירועי `touch`.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — `showOnMap(lat,lng,day)`:
  **הדרך פנימה, שחסרה.** כל כפתורי הניווט שלחו החוצה ל-Google או ל-`geo:` — ובאקמאס
  בלי קליטה שניהם לא עושים כלום, בעוד שהמפה שכן עובדת היא של האפליקציה עצמה. עכשיו
  לכל נקודה יש 📍 שפותח אותה במפה הפנימית ב-zoom 15. `MAP_FOCUS` נצרך פעם אחת כשהמפה
  מוכנה ומתאפס; בחירת יום בצ'יפים מנקה אותו וחוזרת למסגור היום.
  **ובאותה הזדמנות נמצא באג אמיתי שהבעלים הצביע עליו:** ב-`renderItinerary` כפתור
  הניווט היה מגודר ב-`['sightseeing','food','hotel','car'].includes(item.type)`,
  ולכן **פריטי `logistics` ו-`flight` לא קיבלו שום כפתור גם כשהיו להם קואורדינטות
  מדויקות** — איסוף הרייזר, החזרת הרכב, הנחיתה והמונית לשדה. אלה בדיוק הפריטים שהכי
  צריך לנווט אליהם. התנאי עכשיו: קואורדינטה אמיתית תמיד מזכה בכפתורים; רשימת הסוגים
  נשארה רק למקרה שאין קואורדינטה, שם כל מה שהכפתור יכול לעשות הוא חיפוש טקסט בגוגל.
  אומת יום-יום: 28 מתוך 28 הפריטים שיש להם מיקום מקבלים עכשיו כפתור, לעומת 21 לפני.
  `dataVersion` ל-17, `CACHE` ל-`v36`.
  **הערה על אימות קואורדינטות:** ניסיתי לאמת את נקודות אקמאס מול `cyprus.pmtiles`
  דרך רינדור המפה — **זה לא עובד בסביבה הזו**, הקנבס יוצא ריק גם בנקודות שוודאי
  מכוסות. אל תסיק מזה כלום. הדרך היחידה שעבדה עד היום היא לבקש מהבעלים לחיצה ארוכה
  בגוגל מפות.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — שתי הכרעות שהבעלים
  הביא מידע אליהן.
  **Alpha Divers מצביע עכשיו למרינה** (34.9219 / 33.6386) ולא לנקודה שגוגל מחזיר.
  הנקודה של גוגל היא המשרד והמחסן הרשומים שלהם בפילה, כביש דקליה — 7.3 ק״מ מהמרינה
  ו-8.2 ק״מ מהמלון. הסירה לזנוביה יוצאת מהמרינה, שהיא כקילומטר מהמלון, וזה גם מה
  שהיומן תמיד אמר. הכתובת בפילה נשארה בשדה `address` של ההזמנה, כדי שתהיה נגישה אם
  בכל זאת יפנו אותם לשם. **הלקח הכללי: הסיכה של הזמנה צריכה להצביע לאן שמגיעים, לא
  לכתובת הרשומה בטאבו.** משימת ההכנה רוככה מ"איזו משתיהן" ל"לוודא בהודעה".
  **חוף לארה** — הבעלים בירר: מותר להגיע, אסור לרדת עם הרייזר לקו החוף או לחול הרך;
  חונים באזור מוגדר והולכים. האזהרה הישנה ("אולי סגור לרכב") הוחלפה בעובדה.
  `dataVersion` ל-16, `CACHE` ל-`v35`.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — שלושה דברים שהבעלים
  שאל עליהם.
  **(א) `bookings.activities`** — קטגוריה חדשה לצד `flights`/`hotels`/`cars`, גם
  ב-`renderBookings` וגם ב-`BOOKING_PINS`. צלילה או סירה מוזמנת היא מקום שצריך
  להיות בו בשעה, בדיוק כמו מלון — עד עכשיו היא חיה רק בתוך היום. Alpha Divers נכנס
  כהזמנה ראשונה שם.
  **(ב) סתירה אמיתית שהתגלתה מהנקודה ששלח:** גוגל ממקם את Alpha Divers Scuba Diving
  Centre **בפילה**, כשמונה ק״מ מהמלון — בעוד שהיומן כותב "הליכה ל-Alpha Divers
  במרינה (7 דק׳)". המרינה אכן כקילומטר מהמלון, אז אחד מהשניים שגוי, וזו הצלילה של
  2.10 בבוקר. **לא הכרעתי** — נוספה משימת הכנה לאשר מולם לאן להגיע, והאזהרה יושבת
  על ההזמנה עצמה.
  **(ג) `buildChangeHtml()` + `lastUpdate`** — הבעלים שאל למה האפליקציה לא מודיעה לו
  שהנתונים התעדכנו. התשובה: כרטיס הזריעה מופיע רק על פער גרסאות, ואני מקפיד שלא
  ייווצר פער כזה כי לחיצה עליו מוחקת סימונים. כלומר לא הייתה שום אינדיקציה. עכשיו
  לטיול יש `lastUpdate: {at, what}`, ומסך הבית מציג כרטיס ירוק **מעל הכל** שאומר מה
  השתנה ונסגר בלחיצה (נשמר ב-`localStorage` לפי מזהה טיול). **הוא לא מוחק כלום** —
  זו הבחנה שחשוב לשמר. `dataVersion` ל-15, `CACHE` ל-`v34`.
  **כשמעדכנים נתונים מעכשיו: לעדכן גם את `lastUpdate`**, אחרת השינוי שוב יגיע בשקט.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — הבעלים שלח את הנקודה של
  Leonardo (34.914397 / 33.633846). אומתה מול היומן עצמו: 420 מ׳ מכנסיית אגיוס לזרוס
  ו-620 מ׳ מטיילת פיניקודס, שתיהן כבר בטיול. **עכשיו לכל מקום מוזמן בטיול יש סיכה**
  והודעת "אין קואורדינטות" נעלמה. `dataVersion` ל-14, `CACHE` ל-`v33`.
  הערה למי שמחפש קואורדינטות: `WebSearch` מחזיר כתובות אבל כמעט אף פעם לא
  קואורדינטות, ו-`WebFetch` לאתרי מלונות נחסם כאן ב-`EGRESS_BLOCKED`. הדרך המהירה
  היא לבקש מהבעלים לחיצה ארוכה בגוגל מפות. `mapSearchUrl()` נשאר כרשת ביטחון לכל
  מקום עתידי שיגיע עם כתובת ובלי נקודה.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — מסך הבית קיבל את
  `buildTodayNavHtml()`: בזמן הטיול, כרטיס "ניווט להיום" שמרכז כל מקום שאפשר לנסוע
  אליו היום — תחנות היום לפי הסדר, ואחריהן הנקודות הקבועות (מלון, השכרות). הבעלים
  ביקש להגיע לקישור בלי לעבור ליומן. נוסף גם `mapSearchUrl()`: מקום מוזמן עם כתובת
  ובלי קואורדינטות מקבל קישור חיפוש בגוגל מפות לפי הכתובת — גוגל פותרת אותה במדויק,
  וזה עדיף גם על סיכה מנוחשת וגם על שורה שאי אפשר ללחוץ עליה. זה מה שהופך את
  Leonardo לנגיש בלי שיש לו `lat`/`lng`. הקישור מופיע גם בהודעת "אין קואורדינטות"
  במסך המפה. `CACHE` ל-`v32`, הנתונים לא השתנו ולכן `dataVersion` נשאר 13.
  **הערה לתצוגה:** הכרטיס מופיע רק כש-`getTripStatus` מחזיר `during`. כדי לצלם אותו
  לפני הטיול, בנה snapshot עם `startDate`/`endDate` מוזזים סביב היום — זה מה שעשיתי.
- **2026-09-21 — סשן B (claude/barbur-torus-code-v66jy2)** — הרייזר הוזמן, ויום 3.10
  עודכן לספק בפועל: **PR Rentals LTD** בלאצ׳י (לא Sammer), Polaris RZR 1000cc XP,
  €220 כולל מע״מ וביטוח בסיסי, איש קשר Ploutarxos. **החלון הוא 08:30-17:00 — יום
  שלם**, ולכן אזהרת "4 שעות לא מספיקות" נסגרה; הלו״ז נפרס מחדש על פניו. גם שאלת
  הגיל של איתן נסגרה: רישיון B/B1 ושלושת הנהגים רשומים. ההשכרה נוספה ל-`bookings.cars`
  ולכן מופיעה במסך ההזמנות. `estimates` עודכן מ-€200 ל-€220. `dataVersion` ל-12,
  `CACHE` ל-`v30`. הבעלים שלח את הנקודה של PR Rentals (כביש E713, ליד חניית Venetico
  Cafe) והיא נכנסה להזמנה ולשני פריטי האיסוף וההחזרה — אומתה מול מרינת לאצ׳י של
  4.10, קילומטר וחצי משם. `dataVersion` ל-13, `CACHE` ל-`v31`. **פתוח:** ההשתתפות
  העצמית בביטוח, והמלון Leonardo שנשאר המקום היחיד בלי קואורדינטות.
  **מוקש ה-bidi חזר, ונתפס שוב רק בצילום:** `(18, 21, 55)` בהערה עברית הוצג הפוך.
  `innerText` מחזיר סדר לוגי ולכן לא יכול לתפוס את זה — **רק הסתכלות על התמונה**.
  התיקון: להצמיד כל מספר למילה עברית ("איתן בן 18, אורי בן 21") במקום רשימה מופרדת
  בפסיקים, ולהשתמש ב-`·` במקום פסיק בין פרטים טכניים.
- **2026-09-19 — סשן B (claude/barbur-torus-code-v66jy2)** — יום 3.10 נכתב מחדש:
  במקום באגי בפיסורי/טרודוס, לולאת רייזרים בחצי האי אקמאס עם חמש תחנות ממופות
  (Sammer לאצ׳י, מרחץ אפרודיטה, תצפית הבלגונה, חוף לארה, Viklari). הקואורדינטות
  הגיעו מהבעלים ונבדקו גיאומטרית מול נתונים שכבר היו בטיול. `dataVersion` ל-11,
  `CACHE` ל-`v29`. **נמצא באג bidi בצילום המסך** (זוג קואורדינטות בהערה עברית הוצג
  הפוך) — תועד ב"באגים חוזרים" ותוקן בהסרת הזוג מהפרוזה.
- **2026-09-19 — סשן B (claude/barbur-torus-code-v66jy2)** — סבב מפה וניווט לפי
  בקשת הבעלים. `mapPoints` קורא עכשיו את כל קטגוריות ההזמנות דרך `BOOKING_PINS` ולא
  רק מלונות, ולכן השכרת הרכב מופיעה; `mapCoord()` ממיר קואורדינטות למספרים (חלקן
  שמורות כמחרוזות ב-Firestore). מקום מוזמן בלי קואורדינטות מוצג עכשיו ברשימה תחת
  "אין קואורדינטות, ולכן לא על המפה" במקום להיעלם בשקט. נוסף מסך מלא (`mapToggleFull`),
  הוסר כפתור "הצג את היום הזה על המפה" מכל יום ביומן (המפה נפתחת על כל הטיול והצ'יפים
  כבר שם), ובסרגל התחתון "הזמנות" הוחלף בכפתור "הכל" שפותח את המגירה. `CACHE` ל-`v27`,
  `dataVersion` ל-10 (Sixt קיבל את הקואורדינטות של נמל התעופה, שכבר היו ביומן).
  אומת בדפדפן: מסך מלא 390×844 והחזרה ממנו, Sixt ברשימה, אפס שגיאות.
  נוסף גם `mapNavigateAny()` — כפתור 📍 לצד "נווט" ששולח את הנקודה ב-URI תקני מסוג
  `geo:`, כך שאנדרואיד מציע את כל אפליקציות המפות המותקנות (Maps.me, Organic Maps
  וכו׳). זה חשוב בשטח, שם ל-Google אין מסלול ולאפליקציה אופליין כן יש. ב-iOS אין
  מטפל ל-`geo:` והנפילה היא לאותו קישור ווב של כפתור Google. `CACHE` ל-`v28`.
  **פתוח:** ל-Leonardo Boutique Hotel Larnaca אין קואורדינטות.
- **2026-09-19 — סשן B (claude/barbur-torus-code-v66jy2)** — חוקי האבטחה של
  Firestore הוחלפו ותועדו (ראה "מה מגן על הנתונים"). לפני כן כל המסד היה פתוח
  לקריאה, לכתיבה ולמחיקה לכל אחד. אומתו שישה מקרים מול השרת החי: קריאת טיול ורשימת
  הטיולים עוברות, אוספים אחרים מחזירים 403, מחיקה מחזירה 403, ועדכון רגיל עובר.
  בדיקת המחיקה רצה על מסמך שאינו קיים ובדיקת הכתיבה כתבה ערך זהה לקיים, כדי שלא
  ייגעו בנתונים. נראות הריפו הועלתה לדיון ונסגרה: הבעלים בחר להשאירו ציבורי.
  **אין יותר פריטים פתוחים בקובץ הזה.**
- **2026-09-19 — סשן B (claude/barbur-torus-code-v66jy2)** — אחרי שהמסך הציג
  `€undefined × 3` בטלפון של הבעלים: נוסף `tools/preview.mjs`, שפותח את האפליקציה
  בכרומיום אמיתי ומצלם אותה, ושלושת הכללים שנולדו מהתקלה — סדר הדיפלוי (קוד לפני
  נתונים), צילום מסך כתנאי למסירה, ואימות צולב כשלב בתור. הכלי נבדק בפועל: הוא מצלם
  את מסכי קפריסין עם הנתונים החיים, והוא נכשל כמו שצריך כשמסתירים ממנו קובץ פונט.
  לא נגעתי בקוד האפליקציה, ולכן `CACHE` לא עלה.
- **2026-09-19 — סשן B (claude/barbur-torus-code-v66jy2)** — אימתתי את מה שסשן A
  כתב מול הקוד בפועל. רובו מדויק. שלושה תיקונים: הטבלה נקראת `TRIP_SEEDS` ולא
  `DATA_VERSIONS` (שם כזה לא קיים בקוד), טווח בלוק ה-`<script>` מסתיים ב-3566 ולא
  ב-3568 (3568 היא סוף הקובץ), ונוספה האזהרה שכפתור העדכון דורס את היומן, ההכנות
  והציוד. **תיקון נתונים:** הסירה והבאגי בקפריסין תומחרו בטעות לאדם — שניהם נשכרים
  פעם אחת לכל השלושה. `estimates` תומך עכשיו ב-`total` לצד `perPerson`, ההערכה ירדה
  מ-€1,920 ל-€920, `dataVersion` עלה ל-9 ו-`CACHE` ל-`v26`. **פתוח לבעלים:** חוקי
  האבטחה של Firestore, ונראות הריפו (ראה השיחה).
- **2026-09-19 — סשן A (claude/memshiim-mapa-5l88dz)** — עברתי על הטיוטה של סשן B
  והפכתי אותה לנכונה ומלאה מתוך היסטוריית העבודה על הפרויקט. תוקנו: מספר השורות
  (3568), שיטת הפרסום (GitHub Pages, דחיפה ל-main = דיפלוי), אזהרת ה-merge שמחליף
  מערך שלם, מוקש רישום ה-service worker, ומוקש הזריעה מול Firestore. נוספו: סעיף
  "החלטות ומוקשים" (גישות שנפסלו, באגים חוזרים והתיקון, העדפות הבעלים), סעיף
  "מי משתמש בזה", סעיף "נתוני הטיול הפעיל", והערת ה-`pushState` בלי פרמטר URL.
  הסעיף "לא מאומת" מולא ונמחק. **לא נגעתי בקוד האפליקציה — תיעוד בלבד**, ולכן
  `CACHE` לא עלה. **נשאר פתוח לבעלים:** לתעד את חוקי האבטחה של Firestore לאוסף
  `tours` (מה בפועל מגן עליו) — לא ניתן לאמת מהקוד.
- **2026-09-19 — סשן B (claude/barbur-torus-code-v66jy2)** — הקובץ הזה נוצר, ואז
  הועבר לעברית. לא נגעתי בקוד האפליקציה. נכתב מתוך הקוד וההיסטוריה בלבד.
